name: natalo_admin
description: "Natalo Petshop — Admin/Seller app. Shopee Seller-style untuk manage produk, orders, dan operasional toko di HP."
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.5
  url_launcher: ^6.3.1
  cached_network_image: ^3.4.1
  fl_chart: ^0.69.0
  intl: ^0.19.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

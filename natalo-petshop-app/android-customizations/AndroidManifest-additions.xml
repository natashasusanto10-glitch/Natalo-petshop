<!--
  ============================================================
  CARA PAKAI FILE INI:
  ============================================================
  Setelah jalankan `npx cap add android`, edit file:
    android/app/src/main/AndroidManifest.xml

  Tambahkan baris permission di bawah TAG <manifest> (sebelum <application>):
-->

<!-- WAJIB untuk hybrid mode -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<!-- Untuk membuka WhatsApp / dial / email -->
<!-- Letakkan di dalam <manifest> (sejajar dengan <uses-permission>) -->
<queries>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="https" />
    </intent>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="http" />
    </intent>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="tel" />
    </intent>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="mailto" />
    </intent>
    <intent>
        <action android:name="android.intent.action.SENDTO" />
        <data android:scheme="smsto" />
    </intent>
    <package android:name="com.whatsapp" />
    <package android:name="com.whatsapp.w4b" />
    <package android:name="com.instagram.android" />
</queries>

<!--
  ============================================================
  PENTING: Edit juga <application> tag, tambah:
    android:usesCleartextTraffic="false"
    android:networkSecurityConfig="@xml/network_security_config"
  ============================================================
-->

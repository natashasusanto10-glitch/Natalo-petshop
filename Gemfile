source "https://rubygems.org"

# Fastlane handles all iOS automation:
# - match (cert/profile management)
# - gym (build .ipa)
# - pilot (upload TestFlight)
# - app_store_connect_api_key (auth via API key, no Apple ID password)
gem "fastlane", "~> 2.222"

# CocoaPods needed for Capacitor iOS dependencies
gem "cocoapods", "~> 1.15"

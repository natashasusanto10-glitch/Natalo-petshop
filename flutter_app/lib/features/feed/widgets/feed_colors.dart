import 'dart:ui' show Color;

/// Aksen commerce oranye feed — dipakai di badge cart rail aksi
/// (`feed_action_rail.dart`) dan tombol tambah-keranjang kartu produk
/// anchor (`feed_product_anchor_card.dart`). Konstanta bersama supaya
/// kedua tempat selalu identik, tidak lagi duplikat literal warna.
const feedCommerceOrange = Color(0xFFFF7A00);
